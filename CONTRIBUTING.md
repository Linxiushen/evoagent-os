# Contributing

Open an issue for substantial protocol changes. For code changes, add a focused regression test, run `ruff check .`, `ruff format --check .` and `pytest -q`, and explain any security-boundary changes in the pull request. New tools must declare a risk level and JSON Schema.

