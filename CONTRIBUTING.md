# Contributing

Changes to artifact bytes, signature semantics or capability inference require regression fixtures and a compatibility note. Run `ruff check .`, `ruff format --check .` and `pytest -q` before opening a pull request.

