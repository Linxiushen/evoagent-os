# Contributing

Scheduling and lease changes require tests for crash, retry and late completion behavior. Run `ruff check .`, `ruff format --check .` and `pytest -q`. Keep worker APIs backward compatible or document a migration.

